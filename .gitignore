# Ignore AI tooling and editor scaffolding from the published repo.
.github/
**/.instructions.md
.vscode/

# Ignore my personal flight logs but keep the folder so the path exists on clone.
logs/*
!logs/.gitkeep

# Personal-only artefacts (legacy scripts, override files, snapshots).
# Gitignored on both branches so local copies never get committed.
personal/

# Branch-specific artefacts (legacy scripts, override files, snapshots).
# Gitignored on both branches so local copies never get committed.
# programs/bereshit.ks
# programs/bmidbar.ks
# programs/bdmidbar.ks
/_Legacy/

# Misc
Thumbs.db
*.bak