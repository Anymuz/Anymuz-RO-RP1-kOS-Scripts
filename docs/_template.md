# <script name>

Path: `0:/<folder>/<file>.ks`

## Purpose
One or two lines on what the script is for and where it sits in the boot flow or layer stack.

## Imports
- `0:/lib/<other>.ks` (if any)

## Globals declared
- `<name>` - short note. Skip the section if none.

## Functions
- `funcName(arg1, arg2)` - one-line summary of what it does.
- `funcName2()` - one-line summary.

## Notes
Anything mission-design relevant. RO/RP1 caveats, ignition limits, ullage notes, simulation-only assumptions, etc. Optional.
